import React from 'react';
import { WordListType } from '../../../common/types';

export const WordListForm = ({ wordList=undefined, onSave }: { wordList?: WordListType, onSave: ( )=> void }) => 
{
  return (<div></div>)
}