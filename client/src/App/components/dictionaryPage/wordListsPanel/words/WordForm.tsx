import React from 'react';
import { WordListType, WordType } from '../../../common/types';

export const WordForm = ({wordList, word=undefined, onSave}: { wordList: WordListType, word?: WordType, onSave: (word: WordType) => void }) => 
{
  return (<div></div>)
}