import React from 'react';
import { WordListType, WordType } from '../../../common/types';

export const WordForm = ({wordList, word=undefined, onSave}: { wordList: WordListType, word?: WordType, onSave: ()=> void }) => 
{
  return (<div></div>)
}